import { SafeAreaView, Text } from "react-native";
import React from "react";

export default function CartScreen() {
    return (
        <SafeAreaView>
            <Text>cart</Text>
        </SafeAreaView>
    );
}
